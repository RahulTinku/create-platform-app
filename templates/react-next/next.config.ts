import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Strict output for production deployments
  output: undefined,
  // No experimental flags needed — using stable Next.js 15 features only
};

export default nextConfig;
